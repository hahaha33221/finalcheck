# Copy this file to .env and fill in real values before starting the server.

# Port the Node server listens on (put nginx or another reverse proxy in front of it).
PORT=4000

# Plain-text admin password used only once, at first boot, to seed the hash below.
# After the server has started once and written data/admin.json, you can remove this
# line -- the stored bcrypt hash is what's actually checked from then on.
ADMIN_PASSWORD=change-me

# Comma-separated list of origins allowed to call the API (your deployed frontend's
# URL(s)). Add every origin that will call this API -- e.g. the site served by this
# same VPS/nginx AND a separately hosted frontend such as a Vercel deployment.
# Example: http://31.97.71.87,https://your-project.vercel.app
CORS_ORIGIN=http://localhost:5173

# Whether "최종확인" (final confirm / mark a bus as departed and lock it) is
# allowed. Defaults to off (anything other than exactly "true") so the roster
# stays freely editable while it's still being set up; set to "true" and
# restart the service once the team is ready to start locking buses as they
# actually leave.
DEPART_ENABLED=false
